# Critical Code Style Rules

* TypeScript implementation functions MUST rely on TypeScript return-type inference.
* Do not add explicit return type annotations to function or method implementations.
* This is a required project convention, not a preference.
* See `.claude/rules/code-styles.md`.
